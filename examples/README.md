# Isolated examples

Examples are never the default production preset. The historical medical/Notex implementation remains in `apps/medical-web`; fixture manifests used by tests live under `sites` with `fixture: true`. Production validation and output auditing block both paths.
